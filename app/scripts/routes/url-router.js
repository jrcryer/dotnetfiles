/*global define*/

define([
    'jquery',
    'backbone',
], function ($, Backbone) {
    'use strict';

    var UrlRouter = Backbone.Router.extend({
        routes: {
        },

    });

    return UrlRouter;
});